`surround_protocol.json` is copied without modification from the sibling
surround-camera project's authoritative `protocol/fixtures.json`, API 1.0.
Changes to that contract must update and verify Rust, Dart and native readers.
These are synthetic control messages and contain no physical vehicle data.
